'use strict';

/**
 * @ngdoc function
 * @name loopbackExampleFullStackApp.controller:HomeCtrl
 * @description
 * # HomeCtrl
 * Controller of the loopbackExampleFullStackApp
 */
lefsApp.controller('HomeCtrl', function ($scope) {
    $scope.foo = Math.random();
  });
