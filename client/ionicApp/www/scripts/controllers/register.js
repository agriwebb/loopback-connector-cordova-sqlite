'use strict';

/**
 * @ngdoc function
 * @name loopbackExampleFullStackApp.controller:RegisterCtrl
 * @description
 * # RegisterCtrl
 * Controller of the loopbackExampleFullStackApp
 */
lefsApp.controller('RegisterCtrl', function ($scope) {
    $scope.foo = Math.random();
  });
