'use strict';

/**
 * @ngdoc function
 * @name loopbackExampleFullStackApp.controller:LoginCtrl
 * @description
 * # LoginCtrl
 * Controller of the loopbackExampleFullStackApp
 */
lefsApp.controller('LoginCtrl', function ($scope) {
    $scope.foo = Math.random();
  });
